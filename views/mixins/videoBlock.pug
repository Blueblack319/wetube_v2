mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile, controls="true")
        a(href=routes.videoDetail)
            h3.video__title=video.title
            h5.video__views=video.views 